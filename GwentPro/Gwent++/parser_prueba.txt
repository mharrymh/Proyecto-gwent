a            for (int i = 0; i < 10; i++)
            {
                // Single line comment
                /* Multi
                   line
                   comment */
                if (i == 5)
                {
                    string card = "Ace of Spades";
                    bool effect = true;
                    Card myCard = new Card();
                    myCard.Name = card;
                    myCard.Effect = effect;
                }
                else
                {
                    int power = i++;
                    Range range = new Range(1, 10);
                    OnActivation activation = new OnActivation();
                    activation.Power = power;
                    activation.Range = range;
                }
            }